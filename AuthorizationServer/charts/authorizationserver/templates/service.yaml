apiVersion: v1
kind: Service
metadata:
  name: {{ template "authorizationserver.fullname" . }}
  labels:
    app: {{ template "authorizationserver.name" . }}
    chart: {{ template "authorizationserver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authorizationserver.name" . }}
    release: {{ .Release.Name }}
